#include<iostream>
#include<bits/stdc++.h>
using namespace std;

class Solution {
public:

    vector<vector<string>> groupAnagrams(vector<string>& strs) {
        vector<vector<string>> results;
        
    }
};

int main() {
    vector<string> s;
    s.push_back("1");
    s.push_back(s[0]);
    for (string str : s) {
        cout << str;
    }
}
